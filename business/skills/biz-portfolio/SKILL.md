---
name: biz-portfolio
description: >
  Use to sweep every registry project's business state and rebuild the global-business.md roll-up. Triggers on "business portfolio sweep", "which projects have a business case", "business coverage across projects", "rebuild global-business", "who still needs a viability verdict", "portfolio monetization overview". Runs the deterministic business-scan.py evidence lane over all enabled projects, renders the coverage table (verdict / stage / staleness / actuals) via business-rollup.py, flags unassessed projects, and never re-derives anything the scanner JSON does not carry.
---

# biz-portfolio — portfolio business sweep

> Placeholder body — authored in Stage 3 Task 3.3. Frontmatter is final so the
> skill is discoverable and passes structure validation.
