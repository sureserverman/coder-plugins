# noassess

Fixture project with a portfolio home but **no `business/` directory** — the scanner
must report `assessed: false` (a triage gap, not an error). This README only exists so
git tracks the otherwise-empty project dir.
