# Integration Graph

**Last rebuilt:** 2026-06-30

---

## Edges (depends_on → upstream)

- `beta` → `alpha`  — beta consumes alpha's widget API
- `alpha` → `gamma`  — alpha builds on gamma's toolchain
